apiVersion: elbv2.k8s.aws/v1beta1
kind: TargetGroupBinding
metadata:
  name: {{ .Release.Name }}
spec:
  serviceRef:
    name: {{ .Release.Name }}
    port: 80
  targetGroupName: {{ .Values.targetGroupName }}
  targetType: ip